export interface DonutChartDataType {
    fuel: { labels: Array<string>; series: Array<number> };
    energy: { labels: Array<string>; series: Array<number> };
    travel: { labels: Array<string>; series: Array<number> };
    meal: { labels: Array<string>; series: Array<number> };
  }